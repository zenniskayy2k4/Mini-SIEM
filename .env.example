# Ollama AI analyst (optional; rules/ML still work when disabled).
AI_PROVIDER=ollama_cloud
AI_FALLBACK_PROVIDER=
OLLAMA_API_KEY=
OLLAMA_BASE_URL=https://ollama.com/api
OLLAMA_MODEL=gemma4:cloud
OLLAMA_LOCAL_BASE_URL=http://host.docker.internal:11434/api
OLLAMA_LOCAL_MODEL=gemma3:4b

# SQLite is primary; JSON remains a migration/fallback mirror.
SQLITE_READ_ENABLED=true
JSON_READ_FALLBACK_ENABLED=true
JSON_DUAL_WRITE_ENABLED=true

# Deterministic risk contribution ceilings. Defaults total 100.
RISK_WEIGHT_SEVERITY=40
RISK_WEIGHT_ASSET=20
RISK_WEIGHT_THREAT_CONFIDENCE=15
RISK_WEIGHT_TI_REPUTATION=15
RISK_WEIGHT_CORRELATION=5
RISK_WEIGHT_HUMAN_REVIEW=5

# Safe response workflow. Simulation never changes the host.
RESPONSE_MODE=simulation
RESPONSE_TARGET_OS=linux
RESPONSE_APPROVAL_TIMEOUT_SECONDS=900
RESPONSE_PROTECTED_TARGETS=localhost,127.0.0.1,::1,host.docker.internal,root,administrator,1,/,/etc,/usr,/bin,/sbin

# Optional generic or Discord webhook for high-risk incidents.
NOTIFICATION_WEBHOOK_URL=
NOTIFICATION_WEBHOOK_FORMAT=generic
NOTIFICATION_TIMEOUT_SECONDS=3
NOTIFICATION_MAX_ATTEMPTS=2

# Manual external-case export. Select and configure exactly one provider.
CASE_EXPORT_ENABLED=false
CASE_EXPORT_PROVIDER=thehive
CASE_EXPORT_TIMEOUT_SECONDS=5
CASE_EXPORT_MAX_ATTEMPTS=2
THEHIVE_URL=
THEHIVE_API_KEY=
JIRA_URL=
JIRA_USER_EMAIL=
JIRA_API_TOKEN=
JIRA_PROJECT_KEY=
JIRA_ISSUE_TYPE=Task

# GeoIP context uses the keyless HTTPS endpoint only for public/global IPs.
GEOIP_ENABLED=true
GEOIP_ENDPOINT=https://ipwho.is
GEOIP_CACHE_TTL_SECONDS=86400
GEOIP_RATE_LIMIT_PER_SECOND=1
GEOIP_TIMEOUT_SECONDS=3
GEOIP_MAX_ATTEMPTS=2

# Optional IP reputation. Empty key keeps the provider disabled.
ABUSEIPDB_API_KEY=

# Optional hash metadata lookup. Files are never uploaded.
VIRUSTOTAL_API_KEY=

# STIX/TAXII feeds. Offline bundles and normalized indicators live under data/.
STIX_BUNDLE_FILE=
TAXII_COLLECTION_URL=
TAXII_BEARER_TOKEN=
TAXII_FEED_SOURCE=taxii
TAXII_PULL_INTERVAL_SECONDS=3600

# Normalized Windows/Sysmon ingest.
WINDOWS_EVENT_FILE=/app/data/windows_events.jsonl
WINDOWS_COLLECTOR_SECRET=

# Dashboard session security. Leave the secret empty to persist a random key in data/.
DASHBOARD_SESSION_SECRET=
DASHBOARD_COOKIE_SECURE=false

# Optional bearer token for /metrics. Empty leaves it unauthenticated.
METRICS_BEARER_TOKEN=

# Offline maintenance (python -m tools.maintenance).
ALERT_RETENTION_DAYS=90
INGESTION_FAILURE_RETENTION_DAYS=30
LOG_ROTATE_MAX_BYTES=10485760
LOG_ROTATE_BACKUPS=5
